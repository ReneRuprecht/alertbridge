version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/platform/psql/query.sql"
    schema: "internal/platform/psql/schema.sql"
    gen:
      go:
        package: "psql"
        out: "internal/platform/psql"
        sql_package: "pgx/v5"
        overrides:
          - column: "alerts.starts_at"
            go_type:
              type: "time.Time"
          - column: "alerts.resolved_at"
            go_type:
              type: "time.Time"
          - column: "alerts.labels"
            go_type:
              type: "map[string]string"
          - column: "alerts.annotations"
            go_type:
               type: "map[string]string"
