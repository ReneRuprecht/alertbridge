name: Go Backend

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: homelab
    container:
      image: reneruprecht/ci-golang126:latest

    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies
        working-directory: ./backend
        run: go mod tidy

      - name: Run tests
        working-directory: ./backend
        run: go test ./... -v
